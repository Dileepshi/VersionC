package udemyBasics;

public class Hidden_or_invisible {
	
	// isdisplayed() can be used only of the element is visible
	 // it will fail if the element is not visible though it is in the code
	// So instead of using isdisplayed() , use size(). If count is more than 1 than the element is present in the code
	//Frames
	// if you want to jump from frame 1 to a frame 2 which is actually in frame 1 then u can jump from frame 1 to 2
	// if frame 2 is out side the frame 1 , then u have to come back to the default content(normal web page) and go to frame 2
	

}
